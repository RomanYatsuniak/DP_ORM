import { TableMap } from '../common/models/table-map';

export const tableMapsFixture: TableMap[] = [
  { tableName: 't1' } as TableMap,
  { tableName: 't2' } as TableMap,
  { tableName: 't3' } as TableMap,
];
